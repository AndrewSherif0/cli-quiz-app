# cli-quiz-app
A simple but powerful CLI-based Quiz System built with Node.js.  
Users can add different types of questions (Multiple Choice, True/False, Short Answer), take quizzes interactively, and get instant score evaluation.

The app stores questions and results locally using the file system, making it a lightweight backend-style project without a database.
